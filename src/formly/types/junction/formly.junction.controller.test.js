import module from './formly.junction.controller'

describe('Controller[Formly]: Junction', () => {
  var FormlyJunctionCtrl

  beforeEach(angular.mock.module(module))

  beforeEach(angular.mock.inject(function(_FormlyJunctionCtrl_) {
    FormlyJunctionCtrl = _FormlyJunctionCtrl_
  }))

  it('PENDING TESTS')

})
