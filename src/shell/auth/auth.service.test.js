import module from './auth.service';

describe('Service: Auth', () => {
  var AuthService;

  beforeEach(angular.mock.module(module));

  beforeEach(angular.mock.inject(function(_AuthService_) {
    AuthService = _AuthService_;
  }));

  it('PENDING TESTS');

});
