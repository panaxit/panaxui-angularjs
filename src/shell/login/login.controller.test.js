import module from './login.controller';

describe('Controller: Login', () => {
  var LoginCtrl;

  beforeEach(angular.mock.module(module));

  beforeEach(angular.mock.inject(function(_LoginCtrl_) {
    LoginCtrl = _LoginCtrl_;
  }));

  it('PENDING TESTS');

});
