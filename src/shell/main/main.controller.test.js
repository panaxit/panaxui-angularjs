import module from './main.controller'

describe('Controller: Main', () => {
  var MainCtrl

  beforeEach(angular.mock.module(module))

  beforeEach(angular.mock.inject(function(_MainCtrl_) {
    MainCtrl = _MainCtrl_
  }))

  it('PENDING TESTS')

})
