import module from './session.service';

describe('Service: Session', () => {
  var SessionService;

  beforeEach(angular.mock.module(module));

  beforeEach(angular.mock.inject(function(_SessionService_) {
    SessionService = _SessionService_;
  }));

  it('PENDING TESTS');

});
