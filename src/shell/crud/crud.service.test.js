import module from './crud.service';

describe('Service: CRUD', () => {
  var CRUDService;

  beforeEach(angular.mock.module(module));

  beforeEach(angular.mock.inject(function(_CRUDService_) {
    CRUDService = _CRUDService_;
  }));

  it('PENDING TESTS');

});
