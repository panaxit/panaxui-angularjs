import module from './formly.cards.controller';

describe('Controller[Formly]: Cards', () => {
  var FormlyCardsCtrl;

  beforeEach(angular.mock.module(module));

  beforeEach(angular.mock.inject(function(_FormlyCardsCtrl_) {
    FormlyCardsCtrl = _FormlyCardsCtrl_;
  }));

  it('PENDING TESTS');

});
