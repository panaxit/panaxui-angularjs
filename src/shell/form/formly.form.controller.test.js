import module from './formly.form.controller';

describe('Controller[Formly]: Form', () => {
  var FormlyFormCtrl;

  beforeEach(angular.mock.module(module));

  beforeEach(angular.mock.inject(function(_FormlyFormCtrl_) {
    FormlyFormCtrl = _FormlyFormCtrl_;
  }));

  it('PENDING TESTS');

});
