import module from './form.controller'

describe('Controller[View]: Form', () => {
  var FormCtrl

  beforeEach(angular.mock.module(module))

  beforeEach(angular.mock.inject(function(_FormCtrl_) {
    FormCtrl = _FormCtrl_
  }))

  it('PENDING TESTS')

})
