import module from './grid.controller';

describe('Controller[View]: Grid', () => {
  var GridCtrl;

  beforeEach(angular.mock.module(module));

  beforeEach(angular.mock.inject(function(_GridCtrl_) {
    GridCtrl = _GridCtrl_;
  }));

  it('PENDING TESTS');

});
